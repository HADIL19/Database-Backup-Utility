click #Click is a Python library for building command-line tools : Click is a Python library for building command-line tools + validates what it understands
#Click does the parsing, validation, and even auto-generates a --help message.